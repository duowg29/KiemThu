# 3.7.4. Demo Chạy Test Tự Động và Phân Tích Kết Quả

## 3.7.4.1. Tổng Quan

Sau khi đã tích hợp Katalon Runtime Engine (KRE) vào GitHub Actions CI/CD, phần này sẽ trình bày demo thực tế về việc chạy test tự động và cách phân tích kết quả. Demo sẽ minh họa toàn bộ quy trình từ khi trigger workflow đến khi nhận được báo cáo cuối cùng.

**Mục tiêu demo:**
- Minh họa quy trình chạy test tự động hoàn chỉnh
- Hướng dẫn cách xem và phân tích kết quả test
- So sánh kết quả giữa các lần chạy khác nhau
- Đánh giá hiệu quả của hệ thống CI/CD

**📸 Ảnh cần chụp:**
- Sơ đồ tổng quan quy trình demo (từ trigger đến kết quả)

---

## 3.7.4.2. Demo Chạy Test Tự Động

### Bước 1: Trigger Workflow

**Các cách trigger workflow:**

**Cách 1: Push code lên repository**
- Thực hiện commit và push code lên branch chính (main/master)
- Workflow sẽ tự động chạy khi có push event

**Cách 2: Tạo Pull Request**
- Tạo pull request từ branch feature sang branch chính
- Workflow sẽ tự động chạy để kiểm tra code mới

**Cách 3: Chạy thủ công (Manual)**
- Vào tab **Actions** trên GitHub
- Chọn workflow cần chạy
- Click **Run workflow** → Chọn branch → Click **Run workflow**

**Cách 4: Chạy theo lịch (Schedule)**
- Workflow tự động chạy theo lịch đã cấu hình (ví dụ: hàng ngày lúc 2h sáng)

**📸 Ảnh cần chụp:**
- Màn hình GitHub Actions với nút "Run workflow" (workflow_dispatch)
- Màn hình sau khi click "Run workflow" và chọn branch
- Màn hình workflow đã được trigger và đang ở trạng thái "Queued" hoặc "In progress"

---

### Bước 2: Theo Dõi Quá Trình Chạy

**Thực hiện:**
1. Vào tab **Actions** trên GitHub
2. Click vào workflow run đang chạy
3. Xem các jobs và steps đang thực thi
4. Click vào từng step để xem log output chi tiết

**Các giai đoạn chạy:**
- **Queued**: Workflow đang chờ runner sẵn sàng
- **In progress**: Workflow đang chạy
  - Step 1: Checkout code
  - Step 2: Run Katalon Tests (bước chính, mất nhiều thời gian nhất)
  - Step 3: Format and Prepare Excel Report
  - Step 4: Upload Formatted CSV Report
- **Completed**: Workflow hoàn thành (Success hoặc Failure)

**📸 Ảnh cần chụp:**
- Màn hình GitHub Actions với workflow đang chạy (status: In progress)
- Danh sách các steps với trạng thái (queued, in progress, completed)
- Log output của step "Checkout code"
- Log output của step "Run Katalon Tests" (một phần, hiển thị quá trình khởi động KRE)
- Log output real-time trong quá trình test đang chạy (hiển thị test cases đang được thực thi)

---

### Bước 3: Xem Kết Quả Tổng Quan

**Thực hiện:**
1. Sau khi workflow hoàn thành, xem trạng thái tổng quan
2. Kiểm tra exit code và thời gian chạy
3. Xem summary của workflow run

**Các trạng thái có thể:**
- ✅ **Success (màu xanh)**: Tất cả test pass, không có lỗi
- ⚠️ **Failure (màu đỏ)**: Có test fail hoặc lỗi trong quá trình chạy
- ⚠️ **Cancelled (màu xám)**: Workflow bị hủy

**Thông tin hiển thị:**
- Thời gian chạy (duration)
- Số lượng jobs và steps
- Trạng thái từng step (passed/failed)

**📸 Ảnh cần chụp:**
- Màn hình tổng quan workflow run đã hoàn thành (status: Success/Failure)
- Thông tin chi tiết: duration, jobs, steps
- So sánh 2 workflow runs: một success và một failure (để thấy sự khác biệt)

---

## 3.7.4.3. Phân Tích Kết Quả Test

### Bước 4: Xem Test Results Summary trong Logs

**Thực hiện:**
1. Vào step "Run Katalon Tests"
2. Scroll xuống phần cuối của log output
3. Tìm phần "Test Results Summary" hoặc "ANALYZING TEST RESULTS"

**Thông tin trong summary:**
- **Total Tests**: Tổng số test cases đã chạy
- **Passed**: Số test cases pass
- **Failed**: Số test cases fail
- **Errors**: Số test cases có lỗi
- **Skipped**: Số test cases bị bỏ qua
- **Duration**: Thời gian chạy test

**Ví dụ output:**
```
Test Results Summary:
  Total Tests: 10
  Failures: 2
  Errors: 0
  Skipped: 0
```

**📸 Ảnh cần chụp:**
- Log output hiển thị "Test Results Summary" với đầy đủ thông tin
- Phần exit code và kết quả cuối cùng (SUCCESS/FAILURE)

---

### Bước 5: Phân Tích HTML Report

**Thực hiện:**
1. Vào phần **Artifacts** của workflow run
2. Download file HTML report (nếu có) hoặc vào thư mục Reports trong project
3. Mở file `report.html` trong browser

**Nội dung HTML report:**
- **Overview**: Tổng quan kết quả test
  - Tổng số test cases
  - Số test pass/fail
  - Thời gian chạy
  - Biểu đồ tròn (pie chart) thể hiện tỷ lệ pass/fail
- **Test Cases**: Danh sách chi tiết từng test case
  - Tên test case
  - Trạng thái (Passed/Failed)
  - Thời gian thực thi
  - Log messages
  - Screenshots (nếu có)
- **Test Suites**: Kết quả theo từng test suite
- **Execution Log**: Log chi tiết quá trình chạy

**Cách đọc report:**
- Màu xanh: Test pass
- Màu đỏ: Test fail
- Click vào test case để xem chi tiết log và screenshots

**📸 Ảnh cần chụp:**
- HTML report mở trong browser - phần Overview (tổng quan với biểu đồ)
- HTML report - phần Test Cases (danh sách test cases với trạng thái)
- Chi tiết một test case đã pass (hiển thị log và screenshots nếu có)
- Chi tiết một test case đã fail (hiển thị error message và log)

---

### Bước 6: Phân Tích CSV Report

**Thực hiện:**
1. Vào phần **Artifacts** của workflow run
2. Download file `test-results-formatted.csv`
3. Mở file CSV trong Excel hoặc Google Sheets

**Nội dung CSV report:**
- Các cột thông tin:
  - Test Case Name: Tên test case
  - Status: Trạng thái (Passed/Failed)
  - Duration: Thời gian chạy
  - Start Time: Thời gian bắt đầu
  - End Time: Thời gian kết thúc
  - Error Message: Thông báo lỗi (nếu có)
  - Log: Log messages

**Lợi ích CSV report:**
- Dễ import vào Excel để phân tích
- Có thể tạo pivot table để thống kê
- Có thể vẽ biểu đồ để so sánh
- Encoding UTF-8 BOM đảm bảo hiển thị đúng tiếng Việt

**Cách phân tích:**
- Sắp xếp theo Status để xem test nào pass/fail
- Sắp xếp theo Duration để tìm test chạy lâu nhất
- Lọc theo Error Message để tìm các lỗi phổ biến
- Tạo biểu đồ cột để so sánh số lượng pass/fail

**📸 Ảnh cần chụp:**
- File CSV mở trong Excel (hiển thị đầy đủ các cột và dữ liệu)
- Excel với dữ liệu được sắp xếp theo Status
- Biểu đồ trong Excel thể hiện tỷ lệ pass/fail (nếu có)
- Kiểm tra encoding tiếng Việt hiển thị đúng trong Excel

---

### Bước 7: Phân Tích JUnit XML Report

**Thực hiện:**
1. Vào thư mục Reports trong project
2. Tìm file `JUnit_Report.xml`
3. Mở file bằng text editor hoặc XML viewer

**Nội dung JUnit report:**
- Định dạng XML chuẩn JUnit
- Chứa thông tin về test suites và test cases
- Có thể tích hợp với các tool khác (Jenkins, TestRail, etc.)

**Cấu trúc file:**
```xml
<testsuites>
  <testsuite tests="10" failures="2" errors="0" skipped="0">
    <testcase name="Test Case 1" classname="..." time="5.2">
      <!-- Passed test case -->
    </testcase>
    <testcase name="Test Case 2" classname="..." time="3.1">
      <failure message="Assertion failed">
        <!-- Error details -->
      </failure>
    </testcase>
  </testsuite>
</testsuites>
```

**Lợi ích:**
- Chuẩn hóa, dễ tích hợp với các tool CI/CD khác
- Có thể parse bằng script để tự động hóa phân tích
- Hỗ trợ nhiều test suites và test cases

**📸 Ảnh cần chụp:**
- File JUnit_Report.xml mở trong text editor (hiển thị một phần cấu trúc XML)
- Phần testcase đã pass trong XML
- Phần testcase đã fail với thông tin failure trong XML

---

## 3.7.4.4. So Sánh Kết Quả Giữa Các Lần Chạy

### Bước 8: Xem Lịch Sử Workflow Runs

**Thực hiện:**
1. Vào tab **Actions** trên GitHub
2. Xem danh sách tất cả các workflow runs
3. So sánh kết quả giữa các lần chạy

**Thông tin hiển thị:**
- Commit message và author
- Thời gian chạy (duration)
- Trạng thái (Success/Failure)
- Branch và commit hash
- Thời gian chạy (date/time)

**Phân tích xu hướng:**
- Tỷ lệ success/failure qua thời gian
- Test cases nào thường xuyên fail
- Thời gian chạy có tăng/giảm không
- Mối liên hệ giữa code changes và test results

**📸 Ảnh cần chụp:**
- Màn hình danh sách workflow runs (hiển thị nhiều runs với các trạng thái khác nhau)
- So sánh 2-3 workflow runs cạnh nhau (một success, một failure)
- Phần commit message và author của các runs

---

### Bước 9: Phân Tích Metrics và Thống Kê

**Các metrics quan trọng:**

1. **Test Execution Rate:**
   - Số lần test chạy thành công / Tổng số lần trigger
   - Ví dụ: 8/10 = 80% success rate

2. **Test Pass Rate:**
   - Số test cases pass / Tổng số test cases
   - Ví dụ: 8/10 = 80% pass rate

3. **Average Execution Time:**
   - Thời gian chạy trung bình của test suite
   - Giúp đánh giá hiệu suất

4. **Failure Rate by Test Case:**
   - Test case nào fail nhiều nhất
   - Cần ưu tiên fix

5. **Trend Analysis:**
   - Xu hướng pass/fail rate qua thời gian
   - Cải thiện hay suy giảm chất lượng

**Cách tính toán:**
- Dựa vào CSV reports từ các lần chạy
- Import vào Excel và tạo pivot table
- Vẽ biểu đồ để trực quan hóa

**📸 Ảnh cần chụp:**
- Bảng thống kê metrics trong Excel (nếu có)
- Biểu đồ thể hiện test pass rate qua thời gian
- Biểu đồ thể hiện average execution time qua thời gian
- Bảng top test cases thường xuyên fail

---

## 3.7.4.5. Xử Lý Khi Test Fail

### Bước 10: Phân Tích Lỗi và Tìm Nguyên Nhân

**Khi test fail, cần kiểm tra:**

1. **Log Output:**
   - Xem phần error messages trong log
   - Tìm dòng lỗi đầu tiên (root cause)
   - Kiểm tra stack trace

2. **HTML Report:**
   - Xem chi tiết test case đã fail
   - Kiểm tra screenshots (nếu có)
   - Đọc error message và log

3. **CSV Report:**
   - Lọc các test cases có Status = "Failed"
   - Xem Error Message column
   - Tìm pattern chung của các lỗi

4. **JUnit Report:**
   - Xem phần `<failure>` hoặc `<error>` trong XML
   - Đọc message và stack trace

**Các loại lỗi thường gặp:**
- **Element not found**: Không tìm thấy element trên trang
- **Timeout**: Element xuất hiện quá lâu
- **Assertion failed**: Kết quả không đúng như mong đợi
- **Network error**: Lỗi kết nối mạng
- **Configuration error**: Lỗi cấu hình (URL, credentials, etc.)

**📸 Ảnh cần chụp:**
- Log output hiển thị error message chi tiết
- HTML report - chi tiết test case đã fail với error message
- Screenshot của test case fail (nếu có trong report)
- CSV report với các test cases failed được highlight

---

### Bước 11: Sửa Lỗi và Chạy Lại

**Quy trình sửa lỗi:**

1. **Phân tích nguyên nhân:**
   - Đọc kỹ error message
   - Kiểm tra code changes gần đây
   - Xem screenshots để hiểu context

2. **Sửa lỗi:**
   - Sửa test case nếu lỗi do test
   - Sửa application nếu lỗi do bug thực sự
   - Cập nhật test data nếu cần

3. **Commit và Push:**
   - Commit code đã sửa
   - Push lên repository
   - Workflow sẽ tự động chạy lại

4. **Xác nhận fix:**
   - Kiểm tra workflow run mới
   - Xác nhận test đã pass
   - Cập nhật documentation nếu cần

**📸 Ảnh cần chụp:**
- Code đã được sửa (test case hoặc application code)
- Commit message mô tả fix
- Workflow run mới sau khi fix (status: Success)
- So sánh kết quả trước và sau khi fix

---

## 3.7.4.6. Kết Luận và Đánh Giá

### Kết quả đạt được:

Sau khi demo và phân tích, hệ thống CI/CD với KRE đã đạt được:

- ✅ **Tự động hóa hoàn toàn**: Test chạy tự động không cần can thiệp thủ công
- ✅ **Báo cáo đầy đủ**: Có nhiều loại báo cáo (HTML, CSV, XML) phục vụ các mục đích khác nhau
- ✅ **Phân tích dễ dàng**: Dữ liệu dễ import và phân tích trong Excel
- ✅ **Phát hiện lỗi nhanh**: Lỗi được phát hiện ngay sau khi code được push
- ✅ **Theo dõi xu hướng**: Có thể theo dõi chất lượng test qua thời gian

### Đánh giá hiệu quả:

**Ưu điểm:**
- Tiết kiệm thời gian: Không cần chạy test thủ công
- Phát hiện lỗi sớm: Lỗi được tìm thấy ngay trong quá trình phát triển
- Báo cáo chi tiết: Có đầy đủ thông tin để phân tích
- Tích hợp tốt: Hoạt động mượt mà với GitHub Actions

**Hạn chế:**
- Cần thời gian setup ban đầu
- Cần máy runner (self-hosted) hoặc phụ thuộc vào GitHub-hosted runner
- Cần license KRE (có thể tốn chi phí)

**Khuyến nghị cải thiện:**
- Thêm notification khi test fail (email, Slack, etc.)
- Chạy test trên nhiều browser khác nhau
- Tối ưu thời gian chạy bằng cách chạy song song
- Tích hợp với Katalon TestOps để quản lý test tốt hơn

**📸 Ảnh cần chụp:**
- Tổng hợp các metrics và thống kê (bảng hoặc biểu đồ)
- So sánh trước và sau khi tích hợp CI/CD (nếu có)
- Dashboard tổng quan (nếu có tool quản lý test)

---

## Tóm Tắt Danh Sách Ảnh Cần Chụp

### A. Demo Chạy Test (Bước 1-3)
1. Màn hình GitHub Actions với nút "Run workflow"
2. Màn hình sau khi click "Run workflow" và chọn branch
3. Màn hình workflow đã được trigger (status: Queued/In progress)
4. Màn hình GitHub Actions với workflow đang chạy (status: In progress)
5. Danh sách các steps với trạng thái
6. Log output của step "Checkout code"
7. Log output của step "Run Katalon Tests" (khởi động KRE)
8. Log output real-time trong quá trình test đang chạy
9. Màn hình tổng quan workflow run đã hoàn thành (Success/Failure)
10. So sánh 2 workflow runs: một success và một failure

### B. Phân Tích Kết Quả (Bước 4-7)
11. Log output hiển thị "Test Results Summary"
12. Phần exit code và kết quả cuối cùng
13. HTML report - phần Overview (tổng quan với biểu đồ)
14. HTML report - phần Test Cases (danh sách với trạng thái)
15. Chi tiết một test case đã pass
16. Chi tiết một test case đã fail (error message)
17. File CSV mở trong Excel (đầy đủ các cột)
18. Excel với dữ liệu được sắp xếp theo Status
19. Biểu đồ trong Excel thể hiện tỷ lệ pass/fail
20. File JUnit_Report.xml mở trong text editor
21. Phần testcase đã pass trong XML
22. Phần testcase đã fail trong XML

### C. So Sánh và Thống Kê (Bước 8-9)
23. Màn hình danh sách workflow runs
24. So sánh 2-3 workflow runs cạnh nhau
25. Bảng thống kê metrics trong Excel
26. Biểu đồ test pass rate qua thời gian
27. Biểu đồ average execution time qua thời gian
28. Bảng top test cases thường xuyên fail

### D. Xử Lý Lỗi (Bước 10-11)
29. Log output hiển thị error message chi tiết
30. HTML report - chi tiết test case đã fail
31. Screenshot của test case fail
32. CSV report với các test cases failed được highlight
33. Code đã được sửa
34. Workflow run mới sau khi fix (Success)
35. So sánh kết quả trước và sau khi fix

### E. Tổng Kết
36. Tổng hợp các metrics và thống kê
37. Dashboard tổng quan (nếu có)

---

**Lưu ý:** Tất cả ảnh nên có chú thích rõ ràng và được đánh số theo thứ tự trong báo cáo. Ảnh nên có độ phân giải đủ để đọc được text và số liệu. Nên chụp cả màn hình để thấy được context đầy đủ.

